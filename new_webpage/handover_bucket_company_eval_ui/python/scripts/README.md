## Reference scripts (replicas)

These scripts are included for **context and debugging** when integrating the evaluator into a web UI.

- `evaluate.py`: single URL → strict JSON (stdout) + optional cost/citations (stderr)

For bulk evaluation (CSV → JSONL/CSV), sampling, and tracing search behavior, see the original repo’s `scripts/` folder.

