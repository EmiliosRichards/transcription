# === Two Database Connections ===

# 1. MEDIA DB (173.249.24.215) — media_pipeline schema (audio_files, transcriptions)
#    First open SSH tunnel: ssh -L 5433:localhost:5432 emilios@173.249.24.215
MEDIA_DB_URL=postgresql+psycopg2://user:password@localhost:5433/dbname

# 2. DIALFIRE DB (185.216.75.247) — contacts, transactions, recordings, agent_data
#    Direct connection (read-only)
DIALFIRE_DB_URL=postgresql+psycopg2://user:password@185.216.75.247:5432/dialfire

# === OpenAI ===
OPENAI_API_KEY=sk-...
