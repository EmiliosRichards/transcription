You will be given:
- [Target Company Attributes JSON]
- [List of Golden Partner Summaries]
- [Evaluator positives/concerns (German, optional)]

Your job is to pick the best Golden Partner to anchor a phone call: we use the match to build rapport, demonstrate understanding, and increase the prospect's confidence ("wir haben bereits für ein sehr ähnliches Unternehmen produktiv gearbeitet").

Return ONLY a single JSON object (no markdown).

**How to match (important):**
- Do NOT match on shallow keywords. Match on *who we can call* and *why the buying motion is similar*.
- Primary signal (~80%): the **callable target audience / buyer persona** overlap.
  - i.e., the specific account types + roles Manuav would call (contact lists), not just “same industry”.
  - Use target fields like `customer_target_segments`, and if present `callable_account_types` / `callable_buyer_roles`.
  - For partners, treat `target_segments` as the best proxy for callable account types.
- Secondary signal (~15%): similar product/offer type and sales motion (SaaS vs services vs wholesale vs marketplace; demo-led vs quote-led).
- Tertiary (~5%): everything else (size/innovation flags, etc.).
- Use the structured signals when available (e.g., `is_saas_software`, `is_complex_solution`, `is_ai_software`, `targets_specific_industry_type`, size/innovation indicators) to support the match, not to override buyer/audience mismatch.
- If evaluator `concerns` mention key sales blockers (e.g., unclear B2B motion, low ACV, missing buyer persona), treat these as important context for whether a case-study anchor is credible.
- Avoid silly matches (example: do NOT match a taxi service to a logistics software company just because both involve "transport").

**Similarity score (required):**
- `match_score` must be one of: "High", "Medium", "Low".
  - High: strong overlap in buyer persona + problem + sales context (very believable as a case-study anchor).
  - Medium: some overlap, but not perfect (still usable for rapport).
  - Low: weak overlap; only choose if it is still credible enough as a rapport anchor. If even this is not credible, return "No suitable match found".

**No-match rule (allowed):**
- Return `matched_partner_name` = "No suitable match found" ONLY if you cannot find a credible case-study anchor.
- If you return no-match, `match_score` MUST be "Low" and `match_rationale_features` should explain briefly why no credible anchor exists.

**Rationale (German):**
- `match_rationale_features`: 2–4 short bullet-like strings in German explaining the match.
- Focus on why the match helps build trust in the call.
  - The FIRST bullet should explicitly mention the callable target audience overlap (who we would call).

**JSON format:**
{
  "match_score": "High",
  "matched_partner_name": "Partner Name OR No suitable match found",
  "match_rationale_features": ["…"]
}

--------TARGET COMPANY ATTRIBUTES JSON------------
{{TARGET_COMPANY_ATTRIBUTES_JSON_PLACEHOLDER}}
-----------END OF JSON-----------------

--------LIST OF GOLDEN PARTNER SUMMARIES------------
{{GOLDEN_PARTNER_SUMMARIES_PLACEHOLDER}}
-----------END OF SUMMARIES-----------------

--------EVALUATOR POSITIVES (German, optional)------------
{{EVALUATOR_POSITIVES_JSON_PLACEHOLDER}}
-----------END OF LIST-----------------

--------EVALUATOR CONCERNS (German, optional)------------
{{EVALUATOR_CONCERNS_JSON_PLACEHOLDER}}
-----------END OF LIST-----------------

